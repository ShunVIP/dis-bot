# ViPik Discord Bot

Многофункциональный Discord-бот для игрового сервера друзей: экономика, репутация, статистика, пародии на стиль речи участников, Steam-интеграция, сводки дня, поиск по Where Winds Meet KB и административные автоматизации.

## Что умеет бот

### Базовые и развлекательные команды
- `/пинг`, `/кто`, `/сервер`
- `/монетка`, `/шар`, `/кубик`
- `/анекдот`, `/котик`, `/мем`
- `/опрос`

### Игры и мини-игры
- `/кнб`, `/кнб_дуэль`, `/кнб_ход`, `/кнб_отмена`
- `/угадай`
- `/виселица`, `/виселица_старт`, `/виселица_буква`
- `/бж`, `/бж_дуэль`

### Экономика
- `/дэйлик`, `/баланс`, `/перевод`, `/штраф`
- `/магазин`, `/купить_роль`
- `/магазин_добавить`, `/магазин_убрать`
- `/топ_баланс`, `/топ_серии`
- `/налог_настроить`, `/налог_статус`

### Репутация и настроение
- `/репа`, `/антирепа`
- `/топ_репа`, `/история_репы`
- `/мое_настроение`, `/настроение_сегодня`

### Роли и автоматизация сервера
- Временные роли: `/выдать_роль`, `/снять_роль`, `/очистить_сироты`
- Роли по репутации: `/репа_роли`, `/репа_роль_добавить`, `/репа_роль_убрать`, `/репа_роль_постоянная`, `/репа_роль_изменить`, `/репа_роли_вкл`, `/моя_репа_роль`
- Авто-роли по голосовым каналам: `/войс_роли_вкл`, `/войс_роли_статус`

### Статистика сообщений и голоса
- `/индекс_сообщений`
- `/топ_актив`, `/топ_слова`, `/топ_эмодзи`
- `/voice_топ`, `/voice_я`
- `/награды_настроить`, `/награды_статус`

### Ежедневные сводки
- `/итог_дня`
- `/итог_дня_канал`
- `/итог_дня_вкл`

В полночь по МСК бот может публиковать авто-сводку дня: актив, войс, ключевые слова и хокку/фолбэк-описание дня.

### Пародия на стиль речи
- `/пародия`
- `/батл`
- `/коллаж`
- `/эпоха`
- `/тема`
- `/мем_фраза`
- `/профиль_стиля`
- `/дообучить`
- `/профилактика`
- `/список_пользователей`
- `/модели_статус`

Режимы генерации:
- `Мем`: быстрый абсурдный Markov
- `Разум`: более связный Markov
- `Автор`: шаблоны на основе TF-IDF
- `Нейро`: ruGPT-подобный режим при наличии `torch/transformers`

### Сбор данных и фильтры для пародии
- `/собрать_канал`
- `/собрать_сервер`
- `/фильтры_показать`, `/фильтр_добавить`, `/фильтр_удалить`
- `/персона_показать`, `/персона_пересобрать`

### Steam-интеграция
- `/стим_привязать`, `/стим_отвязать`
- `/стим`
- `/стим_вишлист`
- `/стим_общие`
- `/релизы_канал`
- `/релизы_проверить`

Есть фоновая проверка релизов и скидок по Steam wishlist.

### Токсичность и модерация
- Детектор токсичных сообщений в `on_message`
- `/токсики`
- `/токсичность_вкл`
- `/токсичность_порог`
- `/токсичность_канал`

### Дни рождения
- `/др`, `/д-р`
- `/др_ад`, `/д-р_ад`
- `/все_др`, `/когда_др`

Есть отдельный планировщик поздравлений по расписанию.

### Поиск и внешние знания
- `/вики`
- `/пабмед`
- `/wwm_search`

Where Winds Meet KB обновляется отдельной ежедневной задачей.

### Справка
- `/меню`
- `/меню_админ`
- `/ачивки`

## Архитектура

### Точки входа
- `main_file.py` — основной запуск бота, загрузка всех cog-модулей из `fun_slesh/`
- `config.py` — загрузка токена и API-ключей из `KGTD.env`

### Папки
- `fun_slesh/` — slash-команды, listeners и игровые/социальные модули
- `core/` — базовая логика экономики
- `scheduled/` — планировщики фоновых задач
- `utils/` — логирование и вспомогательные вещи
- `wwm_kb/` — пайплайн знаний по Where Winds Meet
- `datebase/` — локальные SQLite-базы
- `models/` — локальные артефакты обучения пародии и нейромоделей

### Планировщики
- поздравления с днём рождения
- ежедневный refresh KB
- Steam wishlist polling
- периодические задачи пародии и репутационных ролей
- автосводки дня

## Технологии

- Python 3.12
- `discord.py`
- `apscheduler`
- `aiohttp`
- `sqlite3`
- `loguru`
- `beautifulsoup4`, `lxml`
- `markovify`
- опционально `torch`, `transformers`, `datasets`

## Конфиг

Бот читает переменные из файла `KGTD.env` в корне проекта.

Пример:

```env
tok=YOUR_DISCORD_BOT_TOKEN
STEAM_API_KEY=YOUR_STEAM_API_KEY
BOT_SERVER_MODE=true
ALLOW_GPT_TRAINING_ON_SERVER=false
ALLOW_FULL_MAINTENANCE_ON_SERVER=false
ALLOW_REMOTE_MODEL_INFERENCE=true
ENABLE_DAILY_MARKOV_RETRAIN_ON_SERVER=false
ENABLE_DAILY_MARKOV_COLLECTION_ON_SERVER=true
DAILY_MARKOV_RETRAIN_HOUR=3
DAILY_MARKOV_RETRAIN_MINUTE=15
WEB_ADMIN_ENABLED=false
WEB_ADMIN_HOST=127.0.0.1
WEB_ADMIN_PORT=8080
WEB_ADMIN_TOKEN=CHANGE_ME
```

В репозиторий этот файл коммитить не нужно.

### Безопасный режим для VPS

По умолчанию сервер должен жить в защитном режиме:
- `ALLOW_GPT_TRAINING_ON_SERVER=false` блокирует `/дообучить` для GPT на VPS
- `ALLOW_FULL_MAINTENANCE_ON_SERVER=false` блокирует `/профилактика` на VPS
- `ALLOW_REMOTE_MODEL_INFERENCE=true` оставляет возможность использовать тяжёлую модель с твоего ПК, если включён bridge

Это значит:
- лёгкие модели `Markovify` и `Persona` можно держать на сервере
- тяжёлое GPT-обучение делается локально на ПК через `train_models.bat`
- VPS может только пользоваться тяжёлой моделью с ПК, когда ты сам это включил

Для ежедневного безопасного цикла на VPS:
- `ENABLE_DAILY_MARKOV_RETRAIN_ON_SERVER=true` включает ежедневное дообучение только `Markovify`
- `ENABLE_DAILY_MARKOV_COLLECTION_ON_SERVER=true` перед этим добирает новые сообщения по чекпоинтам
- `DAILY_MARKOV_RETRAIN_HOUR` и `DAILY_MARKOV_RETRAIN_MINUTE` задают время по МСК

### Мини-панель в браузере

В проект встроена опциональная HTML-панель управления:
- она выключена по умолчанию
- защищается токеном `WEB_ADMIN_TOKEN`
- умеет показывать текущую политику безопасности и включать/выключать использование удалённой тяжёлой модели для текущего процесса

Для включения:

```env
WEB_ADMIN_ENABLED=true
WEB_ADMIN_HOST=127.0.0.1
WEB_ADMIN_PORT=8080
WEB_ADMIN_TOKEN=LONG_RANDOM_TOKEN
```

После запуска бот поднимет панель по адресу:

```text
http://IP_СЕРВЕРА:8080/?token=LONG_RANDOM_TOKEN
```

Для реальной безопасности лучше:
- открывать панель только через Tailscale
- или ограничить доступ firewall-правилом по твоему IP
- не использовать короткий токен

## Локальный запуск

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python main_file.py
```

Для Windows уже есть простой `start.bat`, но для постоянного хостинга лучше использовать Linux VPS и `systemd`.

## Данные и большие файлы

- `models/` добавлен в `.gitignore`
- Базы, логи и локальные артефакты не должны храниться в Git
- Если пародийные модели нужны на VPS, они будут обучаться уже на сервере и сохраняться там локально

## Деплой на VPS

В репозитории уже подготовлены:
- `.github/workflows/deploy.yml` — автодеплой по `push` в `main`
- `scripts/deploy.sh` — удалённый сценарий обновления
- `scripts/bootstrap_vps.sh` — первичная настройка Ubuntu/Debian VPS
- `scripts/sync_training_to_vps.ps1` — перенос обученных моделей и persona-баз на VPS без git
- `scripts/model_bridge_server.py` — локальный API для тяжёлых моделей на ПК
- `scripts/run_model_bridge.ps1` — запуск локального model bridge на Windows
- `train_models.bat` — локальное обучение моделей в пару кликов
- `scripts/sync_messages_from_vps.ps1` — скачать актуальную `messages.db` с VPS на ПК перед локальным GPT-обучением
- `enable_heavy_models.bat` / `disable_heavy_models.bat` — включить/выключить тяжёлую модель с ПК для VPS
- `core/admin_panel.py` — безопасная мини-панель управления через браузер
- `deploy/systemd/vipik-discord-bot.service.template` — шаблон сервиса
- `deploy/systemd/vipik-discord-bot-update.*.template` — опциональный таймер автообновления с сервера

### Как это работает

1. Ты пишешь код локально.
2. Делаешь `git push origin main`.
3. GitHub Actions подключается к VPS по SSH.
4. На VPS выполняется `scripts/deploy.sh`.
5. Скрипт обновляет репозиторий, ставит зависимости и перезапускает `systemd`-сервис.

Для приватного репозитория основной путь — GitHub Actions загружает код на VPS по SSH. Таймер автообновления с сервера имеет смысл только если у VPS уже есть отдельный доступ к репозиторию.

### Что нужно один раз сделать на VPS

1. Установить Linux-доступ по SSH.
2. Склонировать репозиторий или запустить `scripts/bootstrap_vps.sh`.
3. Создать `KGTD.env` в каталоге приложения.
4. Настроить GitHub Secret'ы:
   - `VPS_HOST`
   - `VPS_USER`
   - `VPS_SSH_KEY`
   - `VPS_PORT` (опционально)
   - `VPS_APP_DIR` (опционально, по умолчанию `/opt/dis-bot`)
   - `VPS_SERVICE_NAME` (опционально, по умолчанию `vipik-discord-bot`)

### Важно

- Для автодеплоя лучше использовать SSH-ключ, а не пароль
- Если репозиторий приватный, серверу тоже нужен доступ на чтение
- `KGTD.env`, базы и `models/` должны жить на VPS отдельно от Git-истории

## Примечания

- `config.py` сейчас ожидает файл `KGTD.env` именно в корне проекта
- многие социальные функции пишут данные в SQLite в папку `datebase/`
- нейрорежим пародии зависит от наличия `torch` и `transformers`; без них бот продолжит работать, но без GPT-части
