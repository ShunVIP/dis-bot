Do not put real SSH keys into git.

The scripts expect the VPS key at:

%USERPROFILE%\.ssh\disbot_vps_ed25519

If you only use this archive as a transfer bundle, copy the real key manually on the main PC into the Windows user .ssh folder, not into the repository.

